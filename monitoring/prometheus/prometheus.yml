global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: toscan-query
    metrics_path: /metrics
    static_configs:
      - targets: ["query:8081"]
        labels:
          service: toscan
          component: query
